defmodule KafkaExGenStageConsumerTest do
  use ExUnit.Case
  doctest KafkaExGenStageConsumer

  test "I have no tests and I should feel bad :(" do
    # assert KafkaExGenStageConsumer.hello() == :world
  end
end
